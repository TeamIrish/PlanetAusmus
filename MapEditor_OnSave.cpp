#include "MapEditor.h"


// Save game
void MapEditor::OnSave() {
  /*
  for(int a=0;a<4;a++){
	ofstream myfile;
	myfile.open(filenameSave[a].c_str());
	if(myfile.is_open()==false){
		cout << "Could not open file.\n";
	}

	int i=0;
	for(int Y=0;Y<MAP_HEIGHT;Y++) {
		for(int X=0;X<MAP_WIDTH;X++) {
			myfile << gameMap[a].TileList[i].TileXID << ":" << gameMap[i].TileList[i].TileYID << " ";
			i++;			
		}
		myfile << "\n";
	}

	myfile.close();
  }
  */
}
